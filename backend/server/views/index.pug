html
    head
        title= title
    body
        h1= message
        each val in values
            form(action="/admin", method="POST")
                p= (val.name + " - " + val.id)
                input(type="hidden" value=val.id)
                input(type="submit", value="Activate")
        else
            p There are no courses to be activated.